# Замер AI-ответов: что включить

Инструкция для владельца. Приложение, которое делает замеры, живёт в репозитории
`parkourcafe/selena-ai-visibility` и разворачивается на Railway. Сайт
`selenasystems.com` тут ни при чём.

## Новый ключ не нужен

`OPENROUTER_API_KEY` на воркере уже стоит, и адаптер замеров читает именно его —
ту же самую переменную, что и остальные части приложения:

- провайдерский реестр Elmo (`packages/lib/src/providers/registry/openrouter*.ts`);
- онбординг-LLM;
- адаптер замеров Selena.

Менять его не нужно. Осталось только включить два переключателя.

## Что включить

Обе переменные на воркере уже заведены — создавать их не нужно, нужно проверить
и при необходимости поменять значения. По умолчанию адаптер читается как `noop`,
а исполнение считается включённым только при точном значении `true`.

Railway → проект `selena-ai-visibility` → сервис **worker** → **Variables** →
три точки справа от строки → **Edit**:

| Имя | Должно быть |
| --- | --- |
| `SELENA_MEASUREMENT_ADAPTER` | `openrouter` |
| `SELENA_MEASUREMENT_ENABLED` | `true` |

Сервис **web** → **Variables**:

| Имя | Должно быть |
| --- | --- |
| `SELENA_MEASUREMENT_ENABLED` | `true` |

Модель задавать не нужно: по умолчанию берётся `anthropic/claude-haiku-4.5` из
каталога. `SELENA_OPENROUTER_MODEL` существует, но менять её стоит только чтобы
замерить другую модель из каталога — замер измеряет ту модель, которую купил
клиент.

После сохранения Railway передеплоит сервисы сам.

## Лимит расходов

Владелец поставил **$10 в неделю** (25 августа 2026). Этого хватает.

Расчёт. Ответ ограничен 1200 токенами вывода (`OPENROUTER_MAX_TOKENS`, по
умолчанию 1200), модель — `anthropic/claude-haiku-4.5`. По её прайсу худший
случай — около **$0.006 за ответ**; реальные ответы короче потолка, поэтому
ждать стоит $0.002–0.004.

Snapshot за $49 — это 25 вопросов × 3 системы = 75 ответов в неделю на проект:

| Что | Ответов в неделю | Потолок в неделю |
| --- | --- | --- |
| Один проект | 75 | ~$0.45 |
| Все семь | 525 | ~$3.15 |

Против $10 это троекратный запас. Цифры **непроверенные** — прайс не удалось
подтвердить живым запросом, openrouter.ai закрыт сетевой политикой среды
помощника. Настоящую цену покажет первый замер, и она же закроет открытый
вопрос себестоимости из `DECISIONS.md`.

Два уточнения, которые легко упустить:

- Ключ общий на всё приложение, поэтому в те же $10 в неделю попадают вызовы
  провайдерского реестра Elmo и онбординг-LLM. Замер расходует бюджет не один.
- Если лимит упрётся, замер не замолчит: OpenRouter вернёт ошибку, прогон
  запишется как `FAILED` с причиной `PROVIDER_HTTP_402`, и это будет видно в
  админке. За неудавшийся ответ деньги не списываются.

Внутри приложения есть второй пояс: заказ отказывается стартовать, если
оценочная стоимость превышает `PROVIDER_CAP_BLOCKED` или `ORDER_CAP_BLOCKED`.

## Побочный эффект одного ключа

Расходы замеров и расходы остальных частей приложения смешиваются на одном
ключе. Счёт OpenRouter не покажет, сколько стоил именно замер, а настоящая
себестоимость ответа — открытый вопрос из `DECISIONS.md`.

Решается отдельным ключом: адаптер начинает читать `SELENA_OPENROUTER_API_KEY`,
а если её нет — падает обратно на `OPENROUTER_API_KEY`. Тогда у замеров свой
ключ, свой лимит и своя строка в счёте. Это правка в коде на несколько строк;
делать её или нет — решение владельца.

## Открытый вопрос: живой домен смотрит в staging

25 августа на скриншоте владельца видно, что домен `app.selenasystems.com`
привязан к сервису `web` в окружении **staging** проекта `selena-ai-visibility`.
Отчёт о приёмке от 15 августа при этом описывает окружение production с базой
`Postgres-W_9y`, у которой включено резервное копирование PITR.

Если живое приложение работает на staging, надо проверить, куда смотрит его
`DATABASE_URL` и есть ли у той базы резервные копии. Проверить это может только
владелец: помощник до Railway не дотягивается.

## Кто нажимает «запустить»

Замер не стартует по таймеру — только явным действием из админки приложения.
Помощник не может нажать эту кнопку: сетевая политика его среды не пропускает
исходящие соединения ни к Railway, ни к app.selenasystems.com.

Если нужно, чтобы замер запускался кнопкой в GitHub Actions (как отчёт Search
Console), это делается — но тогда в секреты GitHub придётся положить адрес
production-базы. Отдельное решение.

## Чего не делать

Не присылать ключ в переписку с помощником. Всё, что нужно сообщить после
настройки, — что переменные сохранены.
