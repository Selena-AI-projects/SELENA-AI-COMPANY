# EXECUTION_STATE — Content OS + Growth, интеграция и приёмка на staging

Один файл состояния для продолжения после перезапуска сессии. Читать его, а не
повторять аудит с нуля. Обновляется после каждого существенного шага.

Обновлено: 2026-09-07 04:50 UTC.

## Ссылки кода

| Что | Значение |
|---|---|
| Репозиторий | `parkourcafe/selena-OS` |
| Интеграционная ветка | `integration/content-os-growth-ge5` |
| База интеграции | `313daa0c5812d34f8ab7b977f6cc500fde751869` (= `main`, merge PR #28, Content OS Slice 1 + `0037`) |
| Влито | `growth/ge1-4-local-slice` @ `9d7428df794b397d324168620c7c200685f81545` |
| Merge-base | `05599f98434a8ee66496d5dfe1f523ddde98a4b8` |
| HEAD интеграции (финальный, приёмка) | `fed46d4617db0540c344dd73735374cb7a7b090e` (38 коммитов над базой) |
| HEAD для слияния в `main` | `b81c9e7b02bf5b3ca6fb65194e93227a06a2be5e` — merge `main` (`2a8168d7`, Slices 2–3) в growth-линию, GD-14 |
| Aether (финальный) | `Selena-AI-projects/Aether-Medium`, ветка `growth/ge1-4-local-slice` @ `18289d46fa78f7184545518f6aba7484834de45b` |

Репозитории переехали в организацию `Selena-AI-projects` 2026-09-07 ~02:27 UTC;
старые адреса `parkourcafe/*` больше не разрешаются ни для GitHub, ни для Railway.

Поправка к фактам ТЗ: ветка `claude/new-session-r64y7u` откатилась на `05599f98`,
а staging работает на `313daa0c`, который лежит в `main`. Автодеплой web и
receiver с этой ветки вернул бы код **старее** развёрнутого, против схемы с
применённой `0037`.

## Состояние staging (проект `selena-os-staging`, окружение `production`)

Состояние на 2026-09-07 04:20 UTC (ветка `integration/content-os-growth-ge5`):

| Сервис | Развёрнуто | Деплой | Источник | Автодеплой |
|---|---|---|---|---|
| `web` | **`fed46d46`** (финальный SHA), `GROWTH_ENGINE_STAGE1_ENABLED=true` | `db51fbed` SUCCESS 04:24 | `Selena-AI-projects/selena-OS` | **выключен** |
| `projection` | `bc909406`, `GROWTH_ENGINE_STAGE1_ENABLED=true` (код воркера не менялся до `9cdfc675`) | `88310594` SUCCESS 03:48 | `Selena-AI-projects/selena-OS` | **выключен** |
| `migrate` | `a9f79560`, команда — инспектор | `4dc63301` SUCCESS 04:14 (инспектор после `0041`; сама миграция — `ef13d811` 04:12) | `Selena-AI-projects/selena-OS` | **выключен** |
| `receiver` | `1a0a1aa3` (код receiver не менялся после) | `ee6a9fdb` SUCCESS | `parkourcafe/selena-OS` (старый путь; пересборка потребует перепривязки) | **выключен** |
| `worker`, `gateway`, `Postgres` | не трогались | — | — | не было и нет |

Перепривязка `web`, `projection`, `migrate` и двух временных runner-ов на новый
адрес была необходима: по старому пути Railway не читал `railway.json` и не
собирал образы. Каждая перепривязка создаёт trigger автодеплоя — все сняты
(проверено по всем девяти сервисам: 0 триггеров). Обратно сам не включаю.

Команда запуска `migrate` оставлена инспектором: если что-то всё же дойдёт до
сервиса, он прочитает состояние, а не применит миграции.

## Схема

Миграции применены 2026-09-06T05:12:24 UTC вручную по интеграционному SHA.

| Что | Значение |
|---|---|
| Журнал | 38 → 41 → **42**, самая поздняя отметка `1788620580000` |
| Применены | `0038` (`1788620400000`), `0039` (`1788620460000`), `0040` (`1788620520000`), `0041` (`1788620580000`, 2026-09-07 04:12:34 UTC по `a9f79560`, резервная копия тома `bafef901` перед ней) — хеши записей совпадают с файлами |
| Ожидающие / молча пропускаемые | нет / нет |
| Чужие записи | нет: `0037` теперь часть этого checkout |
| Объекты | все шестнадцать проверок инспектора `yes` |
| Рантайм-логины | `web`, `worker`, `gateway`, `ingestion` — провижинены, каждый проверен своим подключением |
| Данные после миграции | 1 организация, 1 бренд, 5 событий, 0 политик, 0 bindings |
| Резервные копии тома | `963c2c4b-…` (2026-09-06 02:29 UTC), `4c8891a1-…` (2026-09-06 04:04 UTC, уже с `0037`), `bafef901-…` (2026-09-07 04:08 UTC, перед `0041`); все бессрочные |

Историю не переписывал: применённые файлы, хеши и отметки прежних миграций не менялись.

## Сделано

- **Интеграция.** Три конфликта разрешены по смыслу: журнал — объединение по тегу
  с сохранением отметок обеих сторон (40 записей, строго по возрастанию);
  `content-workflow-repositories.ts` — совпало имя, а не ответственность, поэтому
  профиль Slice 1 остался на своём пути, а проекция Growth уехала в
  `content-workflow-projection.ts`; манифест пакета — объединение без изменения
  значений.
- **Проверено исполнением.** Typecheck `lib`/`worker`/`web` чист; 764 теста
  проходят; все 40 миграций применяются с нуля; реальный путь обновления staging
  (38 → 40) отрепетирован на копии фактического состояния, повтор — no-op.
- **O18 закрыт.** Миграция `0040` даёт политике контента жизненный цикл: одна
  версия в силе, прежние сохраняются, отозванная не восстанавливается. Две
  SECURITY DEFINER-функции проверяют owner-сессию в самой базе, аудит пишет
  actor/версию/время, повтор идемпотентен. Карточка в Control Room рядом с
  Aether sources. Пять интеграционных тестов под настоящим RLS через web-рантайм.
- **Два независимых review** на промежуточных SHA: первый — 3 BLOCKER и 8 MAJOR,
  второй — 2 MAJOR и 8 MINOR. Все закрыты и перепроверены исполнением.

## Тестовый контур

| Что | Значение |
|---|---|
| Тестовый credential | отдельная переменная receiver `SELENA_AETHER_BRIDGE_TEST_SECRET`, значение сгенерировано Railway, runner берёт его ссылкой `${{receiver.…}}`; production-секрет не читался и не менялся |
| Ограничение | `SELENA_AETHER_BRIDGE_TEST_PROJECTS` — ключ принимается только для перечисленных проектов; секрет без списка = отказ старта |
| Runner | сервис `aether-test-runner-temp` (`6bbfd548-c141-4dd8-9e22-299ea522b151`), образ из `backend/Dockerfile.test-runner`, Postgres внутри контейнера, без тома, restartPolicy NEVER |
| Синтетический проект Aether | `3b8c9d1e-5a2f-4c7b-8e6d-9f0a1b2c3d4e` — задаётся `AETHER_GROWTH_FIXTURE_PROJECT_ID`, одинаков между запусками |
| Отрицательная проверка | проект вне списка: три POST → три **401** в http-логах receiver, ни одно событие не принято |
| Положительная проверка | проект в списке, деплой `7743e352-…`: `claimed 3, sent 3, failed 0`, «every event was accepted by the receiver», расход `tokens_in=0 tokens_out=0 cost=0` |
| Приём на staging | `selena_ingest_raw.aether_events` 5 → **8**; политик, bindings и материалов по-прежнему 0 — события отложены, а не спроецированы, потому что источник ещё не подтверждён |

Деплой runner-а по SHA через API невозможен: токен сессии не видит
`parkourcafe/aether-medium`, сборка идёт только через привязку источника под
GitHub-связкой владельца. На релиз `selena-OS` это не влияет.

## Осталось

Продуктовой разработки больше не требуется — проверено по коду:

- **Бесплатное создание бренда уже существует.** `apps/web/src/server/brands.ts`
  создаёт бренд после проверки URL и вставки, без `analyze-brand`, скрейпинга и
  платного провайдера. A02 не требует новой функции.
- **O18 закрыт** миграцией `0040` и карточкой в Control Room.

### Блокировка: доступ к GitHub пропал

2026-09-07 ~02:15 UTC доступ сессии к GitHub перестал работать: `git push` и
`git fetch` отвечают **403**, GitHub API — **404** на все репозитории проекта,
включая `selena-ai-visibility`. При этом сессия аутентифицирована как
`parkourcafe`, а список разрешённых репозиториев не менялся. Значит, отозван или
сузился грант GitHub-приложения, а не права сессии.

Railway собирает образы только из GitHub по SHA, локальную загрузку исходников
не принимает, а сам staging из этого контейнера недоступен по сети. Поэтому
следующий прогон невозможен, пока доступ не восстановлен.

Готовые, но **не отправленные** коммиты (`integration/content-os-growth-ge5`,
локально): `21c7873e`, `0277f256`, `a6f7ccf3`. Последний отправленный SHA —
`78e46937`.

Разрешилось 2026-09-07 02:27 UTC: репозитории переехали в организацию
**`Selena-AI-projects`** (`Selena-AI-projects/selena-OS`,
`…/SELENA-AI-COMPANY`, `…/Aether-Medium`), приложение Claude получило к ним
доступ, все коммиты отправлены (`integration/content-os-growth-ge5` @ `a6f7ccf3`).

Осталась вторая половина того же переезда — **Railway**. Все сервисы staging
привязаны к старому пути `parkourcafe/selena-OS`; деплой по SHA отвечает
`No GitHub installation found for repo: parkourcafe/selena-OS`, а перепривязка
на `Selena-AI-projects/selena-OS` — `User does not have access to the repo`.
Значит, GitHub-приложение Railway не установлено на организацию
`Selena-AI-projects`. Это действие владельца в GitHub/Railway; из сессии оно
недоступно. Пока оно не сделано, ни один сервис staging (`web`, `receiver`,
`projection`, `migrate`, временные) пересобрать нельзя — работающие деплои при
этом продолжают работать.

### Установленный факт: staging принимает ровно один аккаунт

Прогон `d7ec4cce` (режим `accounts`) прочитал состояние: **1 пользователь**
(создан 2026-09-05T00:46 UTC), 1 credential-аккаунт, 1 организация. Регистрация
второго аккаунта отвечает `422 FAILED_TO_CREATE_USER` — это хук local-режима
`user.create.before`, который отклоняет любую регистрацию после первой.

Решение (GD-11): проверка заводит **собственную** учётную запись строкой в базе,
пароль хешируется тем же пакетом `@better-auth/utils@0.4.2`, которым приложение
пароли и проверяет. Вход выполняется штатным `/api/auth/sign-in/email`, всё
проверяемое — бренд, политика, подтверждение источника — идёт через интерфейс.
Аккаунт владельца не используется, сессия не подделывается. Ограничение честное
и попадёт в итоговый отчёт: обойдён продуктовый лимит «один аккаунт на local-
установку», потому что иначе отдельного синтетического владельца на этом стенде
не существует.

### Путь владельца пройден (2026-09-07 02:49 UTC)

Runner `owner-path-check-temp` (`aab1d95a-…`, образ — стадия `owner-path-check-temp`
в `docker/Dockerfile`, сценарий `e2e/staging/owner-path.mjs`, источник
`Selena-AI-projects/selena-OS`, деплой только по SHA). Прогон `54c921f0` @ `26d6f9d3`:

| Шаг | Как | Результат |
|---|---|---|
| Вход | `/api/auth/sign-in/email` штатно, аккаунт `growth-check@synthetic.invalid` | `200` |
| Организация | `growth-check-org`, членство `admin` — прямые записи, как в `e2e/auth-setup.ts` | есть |
| Бренд | форма онбординга `/app/growth-check-org`, сайт `https://growth-check.synthetic.invalid` | `brands.id = growth-check-org` |
| Политика | карточка «Content policy», версия `staging-check-1`, evidence required | `status = active` |
| Источник | карточка «Confirm a source»: `3b8c9d1e-…`, `selena`, `staging` | binding, `revoked = false` |
| Аудит | `selena_audit.audit_events` для бренда | `content.policy_set` 1, `growth.binding_confirmed` 1 |

Одна поправка робота по дороге: значение, введённое до гидратации страницы,
форма отбрасывает («Website URL is required») — ожидание `networkidle` и
проверка значения поля после ввода (`26d6f9d3`).

### Проекция включена, карточки появились (02:56 UTC)

`projection`: `GROWTH_ENGINE_STAGE1_ENABLED=true`, перепривязан на
`Selena-AI-projects/selena-OS` (по старому пути Railway перестал читать
`railway.json` и собирал Railpack'ом без start command — деплой `f10c97d8`
FAILED, ничего не запускалось). Деплой `87870aed` @ `26d6f9d3`, автодеплой снят.
Лог: два `aether material projected`.

Проверочный прогон `0994a01f` @ `c96eb58e` (режим `verify`, только чтение):

- события: 8; два `content.draft_ready` v1 проекта `3b8c9d1e-…` — `projected`,
  attempts 0; шесть `task.result.ready` (v1-совместимые, не материалы) — не
  трогаются проекцией;
- материалы бренда `growth-check-org`: **2**, общий `brief_ref`
  `0aa70000-484c-440f-b1cc-ab6c23ef372f`, `ARTICLE` и `SOCIAL_ADAPTATION`, у
  каждого версия 1 под политикой `staging-check-1`, `created_by =
  service:registry-worker`, статус `DRAFT`;
- материалы из внешних источников под любым другим брендом: **0**;
- Inbox глазами синтетического владельца: 2 строки, помечены
  «Aether · SYNTHETIC_FIXTURE», «Synthetic», «Needs verification», «Pending»;
  Review: 0 строк. Approvals, release, публикации — 0.

### Повторная доставка, жизненный цикл политики, дефект (03:00–03:50 UTC)

- **Повтор (A04).** Aether-runner перепривязан на `Selena-AI-projects/Aether-Medium`,
  добавлен шаг `AETHER_GROWTH_FIXTURE_REDELIVER=1` (`18289d46`): те же конверты
  отправляются второй раз, `attempts=2`, приняты. Проверка: событий 14 = 5 + 3×3,
  материалов 6 = 3 brief × 2, версий > 1 — 0. Повтор строк не добавил.
- **Отзыв политики через интерфейс** (`withdraw` @ `8bf76301`): `staging-check-1`
  → `revoked`, аудит `content.policy_revoked`=1.
- **Дефект GD-12.** Следующий run спроецировался под отозванной политикой:
  `currentPolicyVersion` не смотрел на `status`. Исправлено в `bc909406`
  (`status = 'active'`), регрессионный тест в worker (падает без исправления,
  11/11 с ним), `projection` пересобран (`68054aa6`).
- **Backoff и перезапуск (A09).** Новый run при отозванной политике: шесть
  отложений с удвоением 30 с → 16 мин (таблица в ACCEPTANCE_MATRIX), затем
  `projection` заменён свежим контейнером (`88310594`), срок следующей попытки
  (03:59:24) не трогался.
- **Новая политика** `staging-check-2` через карточку (03:48:28): обе версии в
  таблице, аудит `content.policy_set`=2.
- **Тесты на `bc909406`/`aae380d2`.** web 270/270; lib 764 + интеграционные: Slice 1
  проходит с провиженным `selena_web_login` (`SELENA_WEB_DB_PASSWORD` →
  `SELENA_WEB_DATABASE_URL`), жизненный цикл политики — тест зависел от порядка
  строк одной транзакции, сделан детерминированным (`aae380d2`), 3/3; worker
  проекции 11/11 на свежей базе. Aether: ruff чисто, pytest 397. Локальный
  Postgres в это время один раз умер без записи в логе (stale pid) — перезапущен.

### Завершение (04:00–04:20 UTC)

- Проекция отложенных событий под `staging-check-2` — 03:59:29, свежим
  контейнером, в назначенный срок (A09 закрыт).
- Независимое review `313daa0c..bc909406`: 0 BLOCKER, 3 MAJOR, 4 MINOR (GD-13).
  MAJOR закрыты в `a9f79560`: миграция `0041` (снят прямой INSERT/UPDATE
  web-рантайма на `content_policies`, DO-утверждение), серверная политика для
  черновиков Control Room, предусловие `0040` для других сред. Дельта
  `bc909406..9cdfc675` отдана тому же ревьюеру на подтверждение.
- `0041` применена на staging по `a9f79560` (резервная копия `bafef901`
  перед ней, инспектор до — `0041` единственная ожидающая, после — журнал 42,
  ожидающих нет). `web` пересобран на `a9f79560` (`d84b78ee`).
- Финальная `verify` @ `a9f79560`: 20 событий, 10 материалов (5 brief × 2), brief
  `5ec3b3f0` под `staging-check-2`, внешние эффекты = 0, Inbox 10 / Review 0.
- Уборка кода: временная стадия `owner-path-check-temp` и исключение
  `!e2e/staging` сняты в `9cdfc675`; скрипт `e2e/staging/owner-path.mjs`
  оставлен для следующей приёмки.
- Повторное review дельты `bc909406..9cdfc675`: **0 BLOCKER, 0 MAJOR**, два MINOR
  (черновик Control Room при отозванной политике брал версию клиента; предусловие
  `0040` не было записано в самом репозитории). Оба закрыты в `fed46d46` —
  финальном SHA: `policyVersionInForce` отказывает, если все политики бренда
  отозваны; запись в `docs/control-room/DECISION_LOG.md` с проверочным запросом.
  `web` пересобран на `fed46d46`.

Итог — `FINAL_REPORT_2026-09-07.md`. Временные сервисы владелец удалил
(04:30 UTC, в проекте остались семь штатных).

### Слияние в `main` (04:30–04:50 UTC, по слову владельца)

`main` ушёл на `2a8168d7`: влиты Content OS Slice 2 и 3 с миграциями, чьи `when`
совпали с growth `0040`/`0041`, применёнными на staging. Решение GD-14: growth
первым, Slice 2/3 → `0042`/`0043` с более поздними `when`. Merge-коммит
`b81c9e7b` отправлен в `integration/content-os-growth-ge5`; проверено на
объединённом дереве: 44 миграции с нуля и повтор no-op; на состоянии growth-линии
инспектор показывает ровно `0042`/`0043` ожидающими и применяет только их; pgTAP
0 `not ok`; tsc чист; lib 779 / worker 34 / web 270; интеграционные наборы всех
слайсов и проекции — на объединённой базе зелёные.

**Влито владельцем**: PR #36 «Integration/content os growth ge5» squash-merge →
`main` = `0c2d84db` (поверх PR #35 `6d1b4e82`, другой сессии: `run-pgtap.sh`,
два документа). Журнал на `main`: 44 записи, `0038`–`0043` в нужном порядке.
Отличие `main` от проверенного `b81c9e7b` — только три файла PR #35, продукт не
затронут. Staging **не менялся** (автодеплой выключен): деплой `main` на staging
потребует сначала применить `0042`/`0043` вручную по регламенту — отдельное
решение владельца, авторы Slices 2–5 держали их вне общей базы.

## Границы, действующие всё время

Только staging и синтетические данные. Прикладные LLM/provider calls, approvals
основного сценария, release и публикации = 0. Production Aether, production AI
Visibility, DNS и домены не трогаются. Секреты и строки подключения не выводятся.
`migrate` обратно на автодеплой сам не ставлю.
